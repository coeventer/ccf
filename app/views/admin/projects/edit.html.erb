<%url = @event ? admin_event_project_path(@event, @project) : admin_project_path(@project)%>
<%cancel_link = @event ? admin_event_projects_path(@event) : admin_projects_path%>
<%=render :partial => "form", :locals => {project: @project, :url => url, :cancel_link => cancel_link}%>