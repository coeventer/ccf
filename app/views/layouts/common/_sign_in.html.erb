<div class="modal fade" id="sign_in_modal">
  <div class="modal-dialog">
    <div class="modal-content">
      <div class="modal-header">
        <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
        <h4 class="modal-title"><i class="fa fa-sign-in"></i> Please sign in</h4>
      </div>
      <div class="modal-body">
        <p><%=brand_name%> leverages Facebook, Google, and Meetup to provide identification. We do not do anything with your account besides identify who you are.</p>
      </div>
      <div class="modal-footer">
        <%=link_to google_oauth2_path, class: "btn btn-primary" do%>
          <i class="fa fa-google-plus"></i> Google Sign in
        <% end %>
        <%=link_to facebook_path, class: "btn btn-primary" do%>
          <i class="fa fa-facebook-square"></i> Facebook Sign in
        <% end %>
        <%=link_to meetup_path, class: "btn btn-primary" do%>
          Meetup Sign in
        <% end %>
        <button type="button" class="btn btn-default" data-dismiss="modal">Cancel</button>
        <hr />
        <p>
          Don't have an account with one of the above? You can to <%=link_to "sign up", "https://accounts.google.com/signup"%> for a Google account.
        </p>

        <% if allow_development_provider? %>
          <p>
          <%= link_to '/auth/developer' do %>
            Development login
          <% end %>
          </p>
        <% end %>
      </div>
    </div>
  </div>
</div>
