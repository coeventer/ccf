<div class="modal fade" id="sign_in_modal">
  <div class="modal-dialog">
    <div class="modal-content">
      <div class="modal-header">
        <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
        <h4 class="modal-title"><i class="fa fa-sign-in"></i> Please sign in</h4>
      </div>
      <div class="modal-body">
        <p><%=brand_name%> leverages Facebook and Google to provide identification. We do not do anything with your account besides identify who you are.</p>
      </div>
      <div class="modal-footer">
        <%=link_to google_oauth2_path, class: "btn btn-primary" do%>
          <i class="fa fa-google-plus"></i> Sign in with Google
        <% end %>
        <%=link_to facebook_path, class: "btn btn-primary" do%>
          <i class="fa fa-facebook-square"></i> Sign in with Facebook
        <% end %>
        <%=link_to meetup_path, class: "btn btn-primary" do%>
          Sign in with Meetup
        <% end %>
        <button type="button" class="btn btn-default" data-dismiss="modal">Cancel</button>
      </div>
    </div>
  </div>
</div>
