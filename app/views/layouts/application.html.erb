<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=Edge,chrome=1">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "Campus Codefest" %></title>
    <%= csrf_meta_tags %>

    <!-- Le HTML5 shim, for IE6-8 support of HTML elements -->
    <!--[if lt IE 9]>
      <script src="//cdnjs.cloudflare.com/ajax/libs/html5shiv/3.6.1/html5shiv.js" type="text/javascript"></script>
    <![endif]-->

    <%= stylesheet_link_tag "application", :media => "all" %>
    <%= javascript_include_tag "application" %>
  </head>
  <body>

    <div class="navbar navbar-fluid-top">
      <div class="navbar-inner">
        <div class="container-fluid">
          <a class="btn btn-navbar" data-target=".nav-collapse" data-toggle="collapse">
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </a>
          <%=link_to "Campus Codefest", root_path, :class => "brand"%>
          <div class="container-fluid nav-collapse">
            <ul class="nav">
              <li class="dropdown">
                <a class="dropdown-toggle"
                   data-toggle="dropdown"
                   href="#">
                    Events
                    <b class="caret"></b>
                  </a>
                <ul class="dropdown-menu">
                  <!-- links -->
                  <li style="font-style: italic;">Upcoming</li>
                  <%@future_events.each do |e|%>
                  <li><%=link_to e.title, event_path(e)%>
                  <%end%>
                  <li style="font-style: italic;">Completed</li>
                  <%@past_events.each do |e|%>
                  <li><%=link_to e.title, event_path(e)%>
                  <%end%>                  
                </ul>
              </li>
              <li><%= link_to "About", about_path  %></li>
              <li><%= link_to "Contact", contact_path  %></li>
              <% if current_user %>
              <li class="dropdown">
                <a class="dropdown-toggle"
                   data-toggle="dropdown"
                   href="#">
                    <%=current_user.name%>
                    <%=image_tag current_user.image, height: 25, width: 25%>
                    <b class="caret"></b>
                  </a>
                <ul class="dropdown-menu">
                  <!-- links -->
                  <li>
                    <%= link_to user_path(current_user) do%>
                    <i class="icon-user"></i> Profile
                    <%end%>
                  </li>
                  <li>
                    <%= link_to signout_path do %>
                    <i class="icon-off"></i> Sign out
                    <%end%>
                  </li>
                  <%if current_user.admin?%>
                  <li class="divider"></li>
                  <li><%= link_to "Admin", admin_root_path%></li>
                  <%end%>
                </ul>
              </li>                
              <%end%>
            </ul>
          </div><!--/.nav-collapse -->
        </div>
      </div>
    </div>

    <div class="container-fluid">
      <div class="row-fluid">
 
        <div class="span12">
          <%= bootstrap_flash %>
          <%= yield %>
        </div>
     
      </div><!--/row-->

      <footer>
        <p>&copy; University of Minnesota <%=Time.now.year%></p>
      </footer>

    </div> <!-- /container -->

  </body>
</html>
