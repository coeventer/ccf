<%=semantic_form_for @user, :url => super_admin_user_path(@user) do |f|%>
  <div class="form-group">
    <label>Name</label>
    <p class="form-control-static">
      <%=@user.name%>
    </p>
  </div>
  <div class="form-group">
    <label>UID</label>
    <p class="form-control-static">
      <%=@user.uid%>
    </p>
  </div>
  <%=f.input :department%>
  <%=f.input :admin%>
  <%=f.input :verified%>
  <%=f.action :submit, :button_html => { :value => 'Save Changes', :class => "btn btn-primary" }%> or <%=link_to "Cancel", super_admin_users_path %>
<%end%>
