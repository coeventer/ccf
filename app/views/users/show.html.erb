<div class="row">
  <div class="col-xs-10">
    <h1><%=@user.name%></h1>
    <p class="text-info">
      Registered for <%=@registered%> upcoming <%='event'.pluralize(@registered).sub(/d+s/, '')%> and attended <%=@attended%> past <%='event'.pluralize(@attended).sub(/d+s/, '')%>.
      Submitted <%=@ideas%> ideas.
      Liked <%=@likes%> ideas.
      Helped with <%=@helps%> ideas.
      Commented <%=@comments%> times.
    </p>
    <h2>Member of</h2>
    <%@user.organization_users.each do |user_organization|%>
    <ul>
      <li><%=link_to user_organization.organization.name, root_url(subdomain: user_organization.organization.subdomain)%></li>
    </ul>
    <%end%>
  </div>
  <div class="col-xs-2">
    <%=image_tag @user.image, alt: @user.name, height: 150, width: 150%>
  </div>
</div>
